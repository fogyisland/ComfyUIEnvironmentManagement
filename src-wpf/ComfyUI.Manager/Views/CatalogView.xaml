<UserControl x:Class="ComfyUI.Manager.Views.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ComfyUI.Manager.ViewModels"
             xmlns:views="clr-namespace:ComfyUI.Manager.Views"
             d:DataContext="{d:DesignInstance Type=vm:CatalogViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel>
        <!-- 顶部工具栏:3 列 Grid(标题 | spacer | 操作),跟 env-list 同款 -->
        <Grid DockPanel.Dock="Top" Margin="12,12,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <!-- Column 0: 标题 + 计数 -->
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="节点目录"
                           FontSize="20" FontWeight="Bold"
                           Foreground="{DynamicResource OnSurfaceBrush}" />
                <TextBlock FontSize="11"
                           Foreground="{DynamicResource OutlineBrush}"
                           Margin="0,2,0,0">
                    <TextBlock.Text>
                        <MultiBinding Converter="{StaticResource BoolToEntryCountText}">
                            <Binding Path="HasEntries" />
                            <Binding Path="PagedEntries.Count" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
            <!-- Column 2: 操作行 -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <!-- 搜索框 -->
                <TextBox Width="240" Margin="0,0,8,0"
                         Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialTextBox}" />
                <!-- segmented 视图切换 -->
                <Border BorderBrush="{DynamicResource OutlineBrush}" BorderThickness="1"
                        CornerRadius="4" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="列表"
                                     GroupName="CatalogViewMode"
                                     IsChecked="{Binding IsListMode, Mode=OneWay}"
                                     Style="{StaticResource CatalogSegmentedRadioButton}" />
                        <RadioButton Content="磁贴"
                                     GroupName="CatalogViewMode"
                                     IsChecked="{Binding IsTileMode, Mode=OneWay}"
                                     Style="{StaticResource CatalogSegmentedRadioButton}" />
                    </StackPanel>
                </Border>
                <!-- 操作 buttons -->
                <Button Content="刷新" Margin="0,0,4,0"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource MaterialButton}" />
                <Button Content="取消" Margin="0,0,4,0"
                        Command="{Binding CancelRefreshCommand}"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"
                        Style="{StaticResource MaterialButton}" />
            </StackPanel>
        </Grid>

        <!-- 进度区域:IsBusy 时显示,含 rate limit banner + progress bar + 4 行文本 -->
        <Border DockPanel.Dock="Top" Margin="12,0,12,8"
                Background="{DynamicResource SurfaceVariantBrush}"
                BorderBrush="{DynamicResource OutlineBrush}"
                BorderThickness="1" CornerRadius="4"
                Padding="12,10"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel>
                <!-- rate limit banner(条件显示) -->
                <views:RateLimitBanner DataContext="{Binding RateLimitBanner}" />
                <!-- 进度条 -->
                <ProgressBar Value="{Binding RefreshPercent, Mode=OneWay}" Maximum="100"
                             Height="6" Margin="0,0,0,8" />
                <!-- 4 行实时进度 -->
                <TextBlock Text="{Binding ReadProgress}" FontSize="12"
                           Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                <TextBlock Text="{Binding WriteProgress}" FontSize="12"
                           Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                <TextBlock Text="{Binding VersionProgress}" FontSize="12"
                           Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                <TextBlock Text="{Binding MetadataProgress}" FontSize="12"
                           Foreground="{DynamicResource OnSurfaceVariantBrush}" />
            </StackPanel>
        </Border>

        <!-- 信息条 -->
        <StackPanel DockPanel.Dock="Top" Margin="8,0,8,4">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource ErrorBrush}"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}" />
            <TextBlock Text="{Binding InfoMessage}" Foreground="{DynamicResource SuccessBrush}"
                       Visibility="{Binding InfoMessage, Converter={StaticResource NullToVisibility}}" />
            <TextBlock Text="{Binding ProgressMessage}" Foreground="{DynamicResource OutlineBrush}"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
        </StackPanel>

        <!-- 底部分页 -->
        <Grid DockPanel.Dock="Bottom" Margin="8"
              Visibility="{Binding HasEntries, Converter={StaticResource BoolToVisibility}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="&lt; Prev"
                    Command="{Binding PrevPageCommand}"
                    Style="{StaticResource MaterialButton}" />
            <TextBlock Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Run Text="{Binding CurrentPage, Mode=OneWay}" />
                <Run Text=" / " />
                <Run Text="{Binding TotalPages, Mode=OneWay}" />
            </TextBlock>
            <Button Grid.Column="2" Content="Next &gt;"
                    Command="{Binding NextPageCommand}"
                    Style="{StaticResource MaterialButton}" />
        </Grid>

        <!-- 主区域:左侧列表/磁贴,右侧详情面板 -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="320" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="380" MinWidth="280" />
            </Grid.ColumnDefinitions>

            <!-- 左:列表 / 磁贴 / 空状态 -->
            <Grid Grid.Column="0">
                <!-- T3:list mode card (CatalogRowCardTemplate + CatalogCardItemContainerStyle from T1) -->
                <ListBox Visibility="{Binding IsListMode, Converter={StaticResource BoolToVisibility}}"
                         ItemsSource="{Binding PagedEntries}"
                         SelectedItem="{Binding Selected}"
                         ItemTemplate="{StaticResource CatalogRowCardTemplate}"
                         ItemContainerStyle="{StaticResource CatalogCardItemContainerStyle}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         HorizontalContentAlignment="Stretch"
                         Margin="8" />

                <ScrollViewer Visibility="{Binding IsTileMode, Converter={StaticResource BoolToVisibility}}"
                              VerticalScrollBarVisibility="Auto" Margin="8">
                    <ListBox ItemsSource="{Binding PagedEntries}"
                             SelectedItem="{Binding Selected}"
                             ItemTemplate="{StaticResource CatalogTileTemplate}"
                             ItemContainerStyle="{StaticResource CatalogTileItemContainerStyle}"
                             ItemsPanel="{StaticResource CatalogTileWrapPanel}"
                             BorderThickness="0" Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled" />
                </ScrollViewer>

                <TextBlock x:Name="EmptyStateText" Text="暂无数据，点右上角 刷新"
                           FontSize="16" Foreground="{DynamicResource OutlineBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding HasEntries, Converter={StaticResource InverseBoolToVisibility}}" />
            </Grid>

            <!-- GridSplitter -->
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center"
                          Background="{DynamicResource OutlineBrush}" />

            <!-- 右:详情面板(卡片化 + 分组 + Divider) -->
            <Border Grid.Column="2" Margin="8,8,8,8" Padding="20"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource OutlineBrush}" BorderThickness="1" CornerRadius="8">
                <Grid>
                    <!-- 未选中状态 -->
                    <TextBlock Text="← 请选择节点查看详情"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{DynamicResource OutlineBrush}"
                               Visibility="{Binding HasSelected, Converter={StaticResource InverseBoolToVisibility}}" />

                    <!-- 详情 -->
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  Visibility="{Binding HasSelected, Converter={StaticResource BoolToVisibility}}">
                        <StackPanel>
                            <!-- Group 1: Header — 标题 + install_type pill -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding SelectedTitle}"
                                           FontSize="22" FontWeight="Bold"
                                           Foreground="{DynamicResource OnSurfaceBrush}"
                                           TextWrapping="Wrap" />
                                <Border Grid.Column="1" Style="{StaticResource CatalogInstallTypeBadgeStyle}">
                                    <TextBlock Text="{Binding SelectedInstallType}"
                                               FontSize="10"
                                               Foreground="{DynamicResource OnPrimaryBrush}" />
                                </Border>
                            </Grid>

                            <!-- Divider -->
                            <Border Height="1" Margin="0,12"
                                    Background="{DynamicResource OutlineBrush}" />

                            <!-- Group 2: 项目信息 -->
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="作者:"
                                           FontSize="12" Margin="0,6,16,6"
                                           Foreground="{DynamicResource OnSurfaceBrush}" />
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedAuthor}"
                                           FontSize="12" Margin="0,6"
                                           Foreground="{DynamicResource OutlineBrush}" />
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="开发者:"
                                           FontSize="12" Margin="0,6,16,6"
                                           Foreground="{DynamicResource OnSurfaceBrush}" />
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedDeveloper, Mode=OneWay}"
                                           FontSize="12" Margin="0,6"
                                           Foreground="{DynamicResource OutlineBrush}" />
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="开发起始:"
                                           FontSize="12" Margin="0,6,16,6"
                                           Foreground="{DynamicResource OnSurfaceBrush}" />
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedDevelopedAt, Mode=OneWay}"
                                           FontSize="12" Margin="0,6"
                                           Foreground="{DynamicResource OutlineBrush}" />
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="安装类型:"
                                           FontSize="12" Margin="0,6,16,6"
                                           Foreground="{DynamicResource OnSurfaceBrush}" />
                                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedInstallType}"
                                           FontSize="12" Margin="0,6"
                                           Foreground="{DynamicResource OutlineBrush}" />
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="最后更新:"
                                           FontSize="12" Margin="0,6,16,6"
                                           Foreground="{DynamicResource OnSurfaceBrush}" />
                                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedLastUpdate}"
                                           FontSize="12" Margin="0,6"
                                           Foreground="{DynamicResource OutlineBrush}" />
                            </Grid>

                            <!-- Divider -->
                            <Border Height="1" Margin="0,4,0,12"
                                    Background="{DynamicResource OutlineBrush}" />

                            <!-- Group 3: 版本信息(用户原话:"这个才是最重要的") — 卡片化强调
                                 内含 4 个用户最关心的字段:最新版本 / 版本日期 / 开发者 / 开发起始。
                                 浅背景 + 粗体值让这块信息从其他字段(灰/小)跳出。 -->
                            <Border Background="{DynamicResource SurfaceVariantBrush}"
                                    CornerRadius="6" Padding="14,10"
                                    Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="版本信息"
                                               FontSize="11" FontWeight="Bold"
                                               Foreground="{DynamicResource PrimaryBrush}"
                                               Margin="0,0,0,8" />
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,8">
                                            <TextBlock Text="最新版本" FontSize="10"
                                                       Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                                            <TextBlock Text="{Binding SelectedLatestVersion, Mode=OneWay}"
                                                       FontSize="16" FontWeight="Bold"
                                                       Foreground="{DynamicResource OnSurfaceBrush}"
                                                       Margin="0,2,0,0" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,8">
                                            <TextBlock Text="版本日期" FontSize="10"
                                                       Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                                            <TextBlock Text="{Binding SelectedVersionDate, Mode=OneWay}"
                                                       FontSize="16" FontWeight="Bold"
                                                       Foreground="{DynamicResource OnSurfaceBrush}"
                                                       Margin="0,2,0,0" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,0">
                                            <TextBlock Text="开发者" FontSize="10"
                                                       Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                                            <TextBlock Text="{Binding SelectedDeveloper, Mode=OneWay}"
                                                       FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource OnSurfaceBrush}"
                                                       Margin="0,2,0,0" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
                                            <TextBlock Text="开发起始" FontSize="10"
                                                       Foreground="{DynamicResource OnSurfaceVariantBrush}" />
                                            <TextBlock Text="{Binding SelectedDevelopedAt, Mode=OneWay}"
                                                       FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource OnSurfaceBrush}"
                                                       Margin="0,2,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- v0.6.15.2 hotfix:仓库 URL(id / repository / url / files[0] / reference 5 路 fallback)。
                                 有 URL 时显示可点击 hyperlink,无 URL 时整行隐藏。
                                 从 Group 2 移出来作为独立行;视觉权重降低(版本信息卡片才是主角)。 -->
                            <TextBlock Margin="0,0,0,4"
                                       FontSize="12"
                                       TextTrimming="CharacterEllipsis"
                                       Foreground="{DynamicResource OutlineBrush}"
                                       ToolTip="{Binding SelectedRepositoryUrl}"
                                       Visibility="{Binding SelectedRepositoryUrl, Converter={StaticResource StringNotEmptyToVisibility}}">
                                <Run Text="仓库:" FontWeight="Bold" />
                                <Hyperlink NavigateUri="{Binding SelectedRepositoryUrl}"
                                           RequestNavigate="OnRepoLinkClick"
                                           Foreground="{DynamicResource PrimaryBrush}">
                                    <Run Text="{Binding SelectedRepositoryUrl, Mode=OneWay}" />
                                </Hyperlink>
                            </TextBlock>

                            <!-- Divider -->
                            <Border Height="1" Margin="0,4,0,12"
                                    Background="{DynamicResource OutlineBrush}" />

                            <!-- Group 4: 版本选择 -->
                            <Grid Margin="0,0,0,8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ComboBox Grid.Column="0"
                                              ItemsSource="{Binding SelectedVersions}"
                                              SelectedItem="{Binding SelectedVersion}"
                                              DisplayMemberPath="DisplayLabel"
                                              IsEditable="False"
                                              Style="{StaticResource CatalogVersionComboBoxStyle}" />
                                    <TextBlock Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <Run Text="发布:" Foreground="{DynamicResource OutlineBrush}" />
                                        <Run Text="{Binding SelectedVersionDate, Mode=OneWay}" FontWeight="Bold"
                                             Foreground="{DynamicResource OnSurfaceBrush}" />
                                    </TextBlock>
                                </Grid>
                                <!-- v0.6.16+:on-demand 拉取状态 + 错误提示 + 重试按钮。
                                     LoadVersionsForSelected 看到 node_versions 空 + 有 ref URL
                                     → 自动触发单节点 /releases 拉取;无 token 撞 60/h 限流时
                                     LoadVersionsError 给出原因 + "重试"按钮让用户配置完 token 再试。 -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="加载版本历史中…" FontSize="11"
                                               Foreground="{DynamicResource OutlineBrush}"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding IsLoadingVersions, Converter={StaticResource BoolToVisibility}}" />
                                    <TextBlock Text="{Binding LoadVersionsError, Mode=OneWay}"
                                               FontSize="11" TextWrapping="Wrap"
                                               Foreground="{DynamicResource ErrorBrush}"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding HasLoadVersionsError, Converter={StaticResource BoolToVisibility}}" />
                                </StackPanel>
                            </Grid>

                            <!-- Group 5: Requirements Expander -->
                            <Expander Header="Requirements" Margin="0,8,0,0" IsExpanded="False"
                                      Visibility="{Binding HasPipRequirements, Converter={StaticResource BoolToVisibility}}">
                                <ItemsControl ItemsSource="{Binding SelectedPipRequirements}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="6,4" Margin="0,1"
                                                    Background="{DynamicResource BackgroundBrush}"
                                                    CornerRadius="3">
                                                <TextBlock FontFamily="Consolas" FontSize="11"
                                                           Foreground="{DynamicResource OnSurfaceBrush}">
                                                    <Run Text="{Binding Name}" FontWeight="Bold" />
                                                    <Run Text="{Binding Specifier, TargetNullValue=''}"
                                                         Foreground="{DynamicResource OutlineBrush}" />
                                                </TextBlock>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>

                            <!-- Divider -->
                            <Border Height="1" Margin="0,12"
                                    Background="{DynamicResource OutlineBrush}" />

                            <!-- Group 5: 链接 -->
                            <TextBlock Margin="0,4,0,0" TextWrapping="Wrap">
                                <Hyperlink NavigateUri="{Binding SelectedReferenceUrl}"
                                           RequestNavigate="OnRepoLinkClick"
                                           Foreground="{DynamicResource PrimaryBrush}">
                                    <Run Text="{Binding SelectedReference, Mode=OneWay}" />
                                </Hyperlink>
                            </TextBlock>

                            <!-- Group 6: 描述 -->
                            <TextBlock Text="{Binding SelectedDescription}" Margin="0,12,0,0"
                                       TextWrapping="Wrap" FontSize="13" LineHeight="20"
                                       Foreground="{DynamicResource OnSurfaceBrush}" />

                            <!-- Group 7: 操作 -->
                            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                <Button Content="{Binding DownloadButtonLabel}"
                                        HorizontalAlignment="Left"
                                        Padding="24,8"
                                        Command="{Binding DownloadCommand}"
                                        CommandParameter="{Binding Selected}"
                                        IsEnabled="{Binding Selected.IsInLocalNodeDb, Converter={StaticResource InverseBool}}"
                                        Style="{StaticResource MaterialButton}" />
                                <!-- v0.6.15: 已下载 badge — Selected entry 已下载时显示在按钮右侧。 -->
                                <Border Margin="12,0,0,0" Padding="8,4" CornerRadius="3"
                                        Background="{DynamicResource SecondaryBrush}"
                                        VerticalAlignment="Center"
                                        Visibility="{Binding Selected.IsInLocalNodeDb, Converter={StaticResource BoolToVisibility}}">
                                    <TextBlock Text="已下载" FontSize="11"
                                               Foreground="{DynamicResource OnPrimaryBrush}" />
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>

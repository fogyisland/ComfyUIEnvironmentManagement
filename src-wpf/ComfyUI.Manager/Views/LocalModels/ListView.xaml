<UserControl x:Class="ComfyUI.Manager.Views.LocalModels.ListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DataGrid ItemsSource="{Binding FilteredModels}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              GridLinesVisibility="Horizontal"
              RowHeight="32"
              SelectionMode="Single">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="★" Width="36" CanUserResize="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <ToggleButton IsChecked="{Binding IsStarred, Mode=OneWay}"
                                      Command="{Binding DataContext.ToggleStarCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      CommandParameter="{Binding}"
                                      Content="⭐" />
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="60" />
            <DataGridTextColumn Header="Title" Binding="{Binding DisplayTitle}" Width="*" />
            <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="100" />
            <DataGridTextColumn Header="Size" Binding="{Binding FileSize}" Width="80" />
            <DataGridTextColumn Header="Hash" Binding="{Binding Hash}" Width="140" />
            <DataGridTextColumn Header="CivitAI" Binding="{Binding MatchedDetail.Id, TargetNullValue=—}" Width="70" />
        </DataGrid.Columns>
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="ContextMenu" Value="{StaticResource LocalModelCardContextMenu}" />
            </Style>
        </DataGrid.RowStyle>
    </DataGrid>
</UserControl>
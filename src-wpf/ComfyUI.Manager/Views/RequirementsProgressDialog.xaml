<Window x:Class="ComfyUI.Manager.Views.RequirementsProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ComfyUI.Manager.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:RequirementsProgressViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="装依赖 (requirements.txt)" Height="520" Width="640"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterOwner">
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top" Padding="8" Margin="0,0,0,12"
                Background="#2A2A2A"
                BorderBrush="Gray" BorderThickness="1">
            <TextBlock Text="{Binding StatusText}"
                       FontSize="14" FontWeight="Bold" />
        </Border>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" Command="{Binding CancelCommand}"
                    Style="{StaticResource MaterialButton}"
                    Width="80" />
            <Button x:Name="CloseButton" Content="关闭" Click="OnCloseClicked"
                    Style="{StaticResource MaterialButton}"
                    Width="80" Margin="8,0,0,0" />
        </StackPanel>

        <TextBox Text="{Binding LogTail, Mode=OneWay}"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Courier New"
                 TextWrapping="NoWrap"
                 Style="{StaticResource MaterialTextBox}" />
    </DockPanel>
</Window>

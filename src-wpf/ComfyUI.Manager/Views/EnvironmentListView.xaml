<UserControl x:Class="ComfyUI.Manager.Views.EnvironmentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
            <Button Content="刷新" Command="{Binding RefreshCommand}"
                    Style="{StaticResource MaterialButton}" />
            <Button Content="+ 新建环境" Command="{Binding CreateCommand}"
                    Style="{StaticResource MaterialButton}" Margin="4,0,0,0" />
            <Button Content="基础环境部署" Command="{Binding BaseEnvCommand}"
                    Style="{StaticResource MaterialButton}" Margin="4,0,0,0" />
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" MinHeight="0" />
                <RowDefinition Height="Auto" MinHeight="0" />
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0" ItemsSource="{Binding Environments}"
                  SelectedItem="{Binding Selected}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="120" />
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="60" />
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80" />
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="80" />
                <DataGridTextColumn Header="基础环境" Binding="{Binding BedDisplay}" Width="220" />
                <DataGridTemplateColumn Header="操作" Width="400">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="启动" Margin="2"
                                        Command="{Binding DataContext.StartCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="{Binding DataContext.StartTooltip,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}" />
                                <Button Content="停止" Margin="2"
                                        Command="{Binding DataContext.StopCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="查看日志" Margin="2"
                                        Command="{Binding DataContext.ShowLogCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="装依赖" Margin="2"
                                        Command="{Binding DataContext.InstallRequirementsCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="运行 pip install -r requirements.txt(过滤 torch 行)" />
                                <Button Content="卸载基础环境" Margin="2"
                                        Command="{Binding DataContext.UninstallBaseEnvCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="重置 BedStatus,保留 venv 文件,可重新部署基础环境" />
                                <Button Content="卸载依赖" Margin="2"
                                        Command="{Binding DataContext.UninstallRequirementsCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="卸载 ComfyUI requirements.txt 已装的包(SQLAlchemy/einops/transformers 等,不动 torch 系列)" />
                                <Button Content="安装节点" Margin="2"
                                        Command="{Binding DataContext.InstallNodeCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                                <Button Content="删除" Margin="2"
                                        Command="{Binding DataContext.DeleteCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <!-- env-start 状态面板 -->
            <Border Padding="8"
                    Background="#1E1E1E" CornerRadius="4"
                    Visibility="{Binding StartStatus.IsVisible, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
                <StackPanel DataContext="{Binding StartStatus}">
                    <TextBlock Text="启动状态" FontWeight="Bold" FontSize="14" Foreground="White" />
                    <TextBlock Text="{Binding CurrentStageText}" FontSize="18" Margin="0,4" Foreground="White" />
                    <ItemsControl ItemsSource="{Binding Stages}" Margin="0,4,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="2,0" Padding="6,2" CornerRadius="3" Background="Gray">
                                    <TextBlock Text="{Binding}" Foreground="White" FontSize="11" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ScrollViewer Height="120" Margin="0,8,0,0" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding LogLines}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="11" Foreground="#CCCCCC" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <TextBlock Text="{Binding Error}" Foreground="#FF6B6B" Margin="0,4,0,0" FontWeight="Bold"
                               Visibility="{Binding Error, Converter={StaticResource NullToVisibility}, FallbackValue=Collapsed}" />
                </StackPanel>
            </Border>
            <!-- 装依赖 状态面板 -->
            <Border Margin="0,4,0,0" Padding="8"
                    Background="#1E1E1E" CornerRadius="4"
                    Visibility="{Binding RequirementsStatus.IsVisible, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
                <StackPanel DataContext="{Binding RequirementsStatus}">
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Content="✕"
                                Click="OnRequirementsStatusCloseClicked"
                                Foreground="White" Background="Transparent"
                                BorderThickness="0" Cursor="Hand" Padding="6,0" />
                        <TextBlock Text="装依赖状态" FontWeight="Bold" FontSize="14" Foreground="White"
                                   VerticalAlignment="Center" />
                    </DockPanel>
                    <TextBlock Text="{Binding StatusText}" FontSize="16" Margin="0,4" Foreground="White" TextWrapping="Wrap" />
                    <ScrollViewer Height="120" Margin="0,8,0,0" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding LogLines}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="11" Foreground="#CCCCCC" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <TextBlock Text="{Binding Error}" Foreground="#FF6B6B" Margin="0,4,0,0" FontWeight="Bold"
                               TextWrapping="Wrap"
                               Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}" />
                </StackPanel>
            </Border>
        </StackPanel>
        <!-- 卸载基础环境 状态面板 (v0.6.5.22 T5) -->
        <StackPanel Grid.Row="2" Margin="0,8,0,0">
            <Border Padding="8" Background="#2A1A1A" CornerRadius="4"
                    Visibility="{Binding BaseEnvUninstallStatus.IsVisible, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
                <StackPanel DataContext="{Binding BaseEnvUninstallStatus}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="卸载基础环境" FontWeight="Bold" FontSize="14" Foreground="#FFB8B8" />
                        <Button Grid.Column="1" Content="✕" Width="20" Height="20"
                                Command="{Binding CloseCommand}" />
                    </Grid>
                    <ScrollViewer Height="100" Margin="0,4" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding LogLines}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="11" Foreground="#FFB8B8" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <TextBlock Text="{Binding Error}" Foreground="#FF6B6B" Margin="0,4,0,0" TextWrapping="Wrap"
                               Visibility="{Binding Error, Converter={StaticResource NullToVisibility}, FallbackValue=Collapsed}" />
                </StackPanel>
            </Border>
        </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>

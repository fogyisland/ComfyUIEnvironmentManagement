<UserControl x:Class="ComfyUI.Manager.Views.LocalNodeListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ComfyUI.Manager.ViewModels"
             Background="{DynamicResource BackgroundBrush}">
    <UserControl.Resources>
        <Style x:Key="LocalNodeCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource OutlineBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header bar -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="本地节点" FontSize="18" FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}"
                    MinWidth="80" />
        </Grid>

        <!-- v0.6.15.6:装节点依赖的 inline 状态面板。NodeRequirementsStatus=null 时整个 Border
             Collapsed(ZeroCountToVisibility 是 false 路径);非 null 时显示 StatusText +
             Logs + 取消按钮 + 关闭按钮。 -->
        <Border Grid.Row="1" Margin="0,0,0,12" Padding="12" CornerRadius="6"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource OutlineBrush}" BorderThickness="1"
                Visibility="{Binding NodeRequirementsStatus, Converter={StaticResource NullToVisibility}}"
                DataContext="{Binding NodeRequirementsStatus}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold">
                        <Run Text="装节点依赖:" />
                        <Run Text="{Binding Title, Mode=OneWay}" />
                        <Run Text=" — " />
                        <Run Text="{Binding StatusText, Mode=OneWay}" />
                    </TextBlock>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="取消" Command="{Binding CancelCommand}"
                                MinWidth="60" Margin="0,0,8,0" />
                        <Button Content="关闭" Click="OnCloseNodeRequirementsClicked"
                                MinWidth="60" />
                    </StackPanel>
                </Grid>
                <ScrollViewer x:Name="LogScrollViewer" Grid.Row="1" MaxHeight="300" Margin="0,8,0,0"
                              VerticalScrollBarVisibility="Visible"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding LogLines}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="11"
                                           TextWrapping="NoWrap" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Empty state -->
        <Border Grid.Row="1" x:Name="EmptyState"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource OutlineBrush}" BorderThickness="1"
                CornerRadius="6" Padding="32"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Visibility="{Binding Items.Count, Converter={StaticResource ZeroCountToVisibility}}">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="本地节点目录为空" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,8" />
                <TextBlock HorizontalAlignment="Center" TextWrapping="Wrap" MaxWidth="400"
                           TextAlignment="Center" Foreground="{DynamicResource OnSurfaceVariantBrush}">
                    <Run Text="从节点目录下载节点后会出现在这里;或" />
                    <Run Text="先在 Settings 配置本地节点目录路径" />
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- List -->
        <ListBox Grid.Row="1" x:Name="ItemsList"
                 ItemsSource="{Binding Items}"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Visibility="{Binding Items.Count, Converter={StaticResource InverseZeroCountToVisibility}}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:LocalNodeListItem}">
                    <Border Style="{StaticResource LocalNodeCardStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                           FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1" Text="{Binding BadgeText}"
                                           Foreground="{DynamicResource PrimaryBrush}"
                                           FontSize="12" VerticalAlignment="Center" />
                            </Grid>
                            <TextBlock Grid.Row="1" Margin="0,4,0,0"
                                       FontSize="11" Opacity="0.7">
                                <Run Text="HEAD: " />
                                <Run Text="{Binding HeadShaDisplay, Mode=OneWay}" />
                            </TextBlock>
                            <TextBlock Grid.Row="2" Margin="0,2,0,0"
                                       FontSize="11" Opacity="0.7"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Info.RepositoryUrl}"
                                       Visibility="{Binding RepositoryUrlDisplay, Converter={StaticResource StringNotEmptyToVisibility}}">
                                <Run Text="URL: " />
                                <Run Text="{Binding RepositoryUrlDisplay, Mode=OneWay}" />
                            </TextBlock>
                            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                                <Button Content="复制到 env"
                                        Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding Info}"
                                        MinWidth="100" Margin="0,0,8,0" />
                                <Button Content="删除"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding Info}"
                                        MinWidth="80" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>